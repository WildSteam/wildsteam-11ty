<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>{{ title }}</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Roboto&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f8f1e5;
      color: #2e2b28;
      margin: 0;
      padding: 0;
      line-height: 1.6;
    }
    header {
      background-image: url('/uploads/Forest.webp');
      background-size: cover;
      background-position: center;
      padding: 6rem 2rem;
      color: #fff;
      text-align: center;
    }
    header h1 {
      font-family: 'Playfair Display', serif;
      font-size: 3rem;
      margin: 0;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.4);
    }
    main {
      padding: 2rem;
    }
    .section {
      margin-bottom: 3rem;
    }
    .section h2 {
      font-family: 'Playfair Display', serif;
      font-size: 2rem;
      margin-bottom: 1rem;
    }
    .gallery {
      overflow-x: auto;
      display: flex;
      gap: 1rem;
      padding-bottom: 1rem;
    }
    .gallery img {
      height: 200px;
      flex-shrink: 0;
      border-radius: 12px;
    }
    .cta {
      background-color: #2e2b28;
      color: #fff;
      padding: 2rem;
      text-align: center;
      border-radius: 12px;
    }
    .cta a {
      display: inline-block;
      margin-top: 1rem;
      padding: 0.75rem 1.5rem;
      background: #fff;
      color: #2e2b28;
      text-decoration: none;
      font-weight: bold;
      border-radius: 6px;
    }
    .testimonials {
      display: grid;
      gap: 1.5rem;
      padding-top: 1rem;
    }
    .testimonial {
      font-style: italic;
      background: #eee5d5;
      padding: 1rem;
      border-left: 4px solid #2e2b28;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <header>
    <h1>{{ hero }}</h1>
  </header>
  <main>
    <div class="section content">
      {{ content | safe }}
    </div>

    {% if gallery %}
    <div class="section">
      <h2>Gallery</h2>
      <div class="gallery">
        {% for item in gallery %}
          <img src="{{ item.image }}" alt="Gallery Image" />
        {% endfor %}
      </div>
    </div>
    {% endif %}

    {% if cta %}
    <div class="section cta">
      <h2>{{ cta.heading }}</h2>
      <p>{{ cta.subtext }}</p>
      <a href="{{ cta.link }}">{{ cta.button }}</a>
    </div>
    {% endif %}

    {% if testimonials %}
    <div class="section">
      <h2>Voices From The Wild</h2>
      <div class="testimonials">
        {% for t in testimonials %}
        <div class="testimonial">
          “{{ t.quote }}”<br><strong>- {{ t.author }}</strong>
        </div>
        {% endfor %}
      </div>
    </div>
    {% endif %}
  </main>
</body>
</html>
